# Gradient Boosting - Gradient Boosting (GBM) is a general boosting algorithm 
# that builds an ensemble of trees by fitting a model to the residuals of the previous tree (or weak learner).
# It minimizes the residuals by using gradient descent.
